// What is an array
// Modifiable list
const students = ["Cernan", "Austin", "Teryn", 1, 35];
console.log(students.length);
console.log(
  `The last element in the students array is ${students[students.length - 1]}`,
);
